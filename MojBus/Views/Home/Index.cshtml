@{
    ViewData["Title"] = "Home Page";
}
<div class="text-center">
    <h1>ISKANJE PO POSTAJAH</h1>
</div>
@await Component.InvokeAsync("Stops")

@if (User.Identity.IsAuthenticated)
{
    @await Component.InvokeAsync("FavouriteStops")
}